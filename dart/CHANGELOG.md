## 0.2.1

* Update documentation and library bindings.

## 0.2.0

* Initial release of Dart FFI bindings for Xaor Cryptographic Engine.
* Supports password hashing and verification using native dynamic libraries (`.dll`, `.so`, `.dylib`).
