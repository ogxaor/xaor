name: xaor
description: Dart bindings for the Xaor Cryptographic Engine — an adaptive, memory-hard, chaos-keyed password hashing algorithm.
version: 0.2.0
homepage: https://github.com/ogxaor/xaor

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
